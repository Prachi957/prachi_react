import InputBox from './InputBox'

export {InputBox}

//This separate file is made in big projects where several components needs to be exported and if we directly export from the files name code beacomes heavy and lengthy so first we import all components to this type of js files and then this single file is exported to main app file like "export {file1, file2, ...}"
